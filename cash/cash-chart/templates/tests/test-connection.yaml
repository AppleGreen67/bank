apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cash-chart.fullname" . }}-test-connection"
  labels:
    {{- include "cash-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['bank-cash-chart/actuator/health']
  restartPolicy: Never

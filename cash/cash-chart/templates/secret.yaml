apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cash-chart.fullname" . }}-secret
  labels:
    {{- include "cash-chart.labels" . | nindent 4 }}
type: Opaque
data:
  KC_SECRET: {{ .Values.global.kc_secret | b64enc | quote }}

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cash.fullname" . }}-secret
  labels:
    {{- include "cash.labels" . | nindent 4 }}
type: Opaque
data:
  KC_SECRET: {{ .Values.global.kc_secret | b64enc | quote }}

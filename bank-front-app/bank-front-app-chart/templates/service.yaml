apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-front-app-chart.fullname" . }}
  labels:
    {{- include "bank-front-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "bank-front-app-chart.selectorLabels" . | nindent 4 }}

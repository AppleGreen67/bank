{{- if .Values.test.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bank-front-app-chart.fullname" . }}-test-connection"
  labels:
    {{- include "bank-front-app-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['bank-bank-front-app-chart/actuator/health']
  restartPolicy: Never
{{- end }}
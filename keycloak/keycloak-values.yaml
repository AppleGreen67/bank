image:
  repository: bitnamilegacy/keycloak
#  tag: 26.3.3-debian-12-r0

auth:
  adminUser: admin
  adminPassword: admin
#  managementUser: manager
#  managementPassword: manager123

postgresql:
  image:
    repository: bitnamilegacy/postgresql
  enabled: true
  auth:
    postgresPassword: postgres123
    password: keycloak123
    username: bn_keycloak
    database: bitnami_keycloak


extraEnvVars:
  - name: KEYCLOAK_LOG_LEVEL
    value: DEBUG
  - name: KEYCLOAK_ENABLE_HTTPS
    value: "false"
  - name: KEYCLOAK_PRODUCTION
    value: "true"
  - name: KEYCLOAK_PROXY
    value: "edge"              # Режим "edge" означает, что шифрование только на прокси
  - name: KC_HOSTNAME_STRICT
    value: "false"
  - name: KC_HOSTNAME_STRICT_HTTPS
    value: "false"             # Разрешаем HTTP для hostname
  - name: KC_HTTP_ENABLED
    value: "true"              # Включаем HTTP-листенер

# ВАЖНО: отключаем HTTPS для локальной разработки
#extraEnvVars:
#  - name: KEYCLOAK_PRODUCTION
#    value: "false"
#  - name: KEYCLOAK_HOSTNAME
#    value: "keycloak.local"
#  - name: KEYCLOAK_HTTP_PORT
#    value: "8080"
#  - name: KEYCLOAK_HTTPS_PORT
#    value: "8443"
#  - name: KC_HOSTNAME_STRICT
#    value: "false"
#  - name: KC_HTTP_ENABLED
#    value: "true"
#  - name: KC_PROXY
#    value: "edge"

# Service конфигурация
service:
  type: NodePort
  ports:
    http: 8080
    https: 8443
#    nodePort: 30080
#  port: 8080
  nodePorts:
    http: 30080
#  adminPort: 8443
#  adminNodePort: 30443

# Ingress для доступа снаружи
#ingress:
#  enabled: true
#  hostname: keycloak.local
#  path: /
#  annotations:
#    kubernetes.io/ingress.class: "nginx"
#    nginx.ingress.kubernetes.io/ssl-redirect: "false"
#    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
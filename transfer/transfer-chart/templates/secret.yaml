apiVersion: v1
kind: Secret
metadata:
  name: {{ include "transfer.fullname" . }}-secret
  labels:
    {{- include "transfer.labels" . | nindent 4 }}
type: Opaque
data:
  KEYCLOAK_SECRET: {{ .Values.global.kc_secret | b64enc | quote }}
